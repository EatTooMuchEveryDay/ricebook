import { } from "./actions";

const initialState = {
};

export function ricebookApp( state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
    return state;
}